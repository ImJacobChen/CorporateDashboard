
import { toReadableDate } from 'corporate-dashboard/helpers/to-readable-date';
import { module, test } from 'qunit';

module('Unit | Helper | to readable date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toReadableDate([42]);
  assert.ok(result);
});

